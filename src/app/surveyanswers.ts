import { listLazyRoutes } from "@angular/compiler/src/aot/lazy_routes";

export class Surveyanswers {
   
        id: number;
        surveyId: number;
        answer: string;
        userScore: number;
        topic:string;
    
}
